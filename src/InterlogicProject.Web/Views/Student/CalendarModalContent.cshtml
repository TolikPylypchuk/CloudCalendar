<div class="modal-header">
	<h4 class="modal-title">{{ subjectName }}</h4>
	<button type="button" class="close" aria-label="Close"
			(click)="activeModal.dismiss('')">
		<i class="fa fa-remove" aria-hidden="true"></i>
	</button>
</div>

<div class="modal-body">
	<h5>{{ type }}</h5>
	<p>{{ formatDateTime(dateTime) }}, ауд. {{ formatClassrooms(classrooms) }}</p>
	<p>{{ formatLecturers(lecturers) }}</p>

	<hr />

	<ip-student-modal-homework [classId]="classId">
	</ip-student-modal-homework>

	<hr />

	<ip-student-modal-materials [classId]="classId">
	</ip-student-modal-materials>

	<hr />

	<ip-student-modal-comments [classId]="classId">
	</ip-student-modal-comments>
</div>

<div class="modal-footer">
	<button type="button" class="btn btn-light"
			(click)="activeModal.close('')">
		Закрити
	</button>
</div>
